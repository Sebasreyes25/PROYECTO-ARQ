kind: pipeline
type: docker
name: test-pipeline

steps:
  - name: test-step
    image: alpine
    commands:
      - echo "Pipeline is working!"

trigger:
  event:
    - push
    - pull_request
    - custom
